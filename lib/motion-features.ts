/**
 * Framer Motion LazyMotion Features
 *
 * This file exports the motion features for LazyMotion,
 * reducing the initial bundle size by lazy-loading animations.
 *
 * Using domAnimation (subset) instead of domMax saves ~15KB
 */
export { domAnimation } from "framer-motion";
